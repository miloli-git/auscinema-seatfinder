{
  "name": "auscinema-seatfinder",
  "version": "0.0.0",
  "private": true,
  "description": "Find a cinema seat worth sitting in across Australian cinema chains.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/adapters/*",
    "packages/api",
    "packages/watcher",
    "apps/web"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "tsc -b",
    "lint": "echo \"(lint not configured yet)\""
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
