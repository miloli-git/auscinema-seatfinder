node_modules/
dist/
build/
.turbo/
*.tsbuildinfo
.env
.env.*
!.env.example
.DS_Store
coverage/
*.log
.vite/

# playwright e2e artifacts (generated)
test-results/
playwright-report/
.playwright/

# deploy secrets / runtime
deploy/.env
deploy/watch.config.json

# local-only review output (never publish; issues are the public record)
reviews/
docs/session-log.md

# design review: generated + local-only
design/mock-v1.static.html
design/critique-*.md

# #51: cron-refreshed Event cinema snapshot (host-side, mounted read-only into api/refresh/ingester)
deploy/refdata/
